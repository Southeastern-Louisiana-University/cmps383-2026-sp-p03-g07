# Copy this file to `.env` and fill in real values. Do NOT commit `.env`.
#
# Payments are optional for this school project:
# - If you leave these blank, checkout uses "Demo payment" mode (no real charge).
# - If you set both keys, checkout can use Stripe test mode.

# Stripe (optional)
STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
